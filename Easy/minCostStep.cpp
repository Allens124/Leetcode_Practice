#include <iostream>
#include <vector>
#include <cmath>
using namespace std;

int minCostStep(vector<int> cost)
{
    int size = cost.size();
    int opt[size] = {0};
    
}

int main()
{
    //code;
    return 0;
}